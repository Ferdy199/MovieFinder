<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Base.Theme.MovieFinder" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/primary</item>
        <item name="android:windowSplashScreenBackground">@android:color/transparent</item>
        <item name="android:windowSplashScreenAnimatedIcon">@null</item>
        <item name="android:splashScreenTheme">@style/Theme.Material3.DayNight.NoActionBar</item>
        <item name="android:fontFamily">@font/ptsans_regular</item>
    </style>
</resources>